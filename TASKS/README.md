# TASKS — mesh intake queue

Work reaches the mesh here without Brian pecking one character at a time.

- Drop a task file in `queue/` (copy `_TEMPLATE.md`).
- **BOO_Cursor_** (cloud memory) picks up `machine: cloud` / `machine: either` on its next turn.
- **BOO_Codex** (Mac hands) picks up `machine: mac`.
- Move completed tasks to `done/`.

See `../START_HERE.md` (if present) and `../CLOUDFLARE_HOME.md` for Cloudflare Room 1 (TASK-008).
